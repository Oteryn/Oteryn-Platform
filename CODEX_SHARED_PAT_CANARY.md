# Codex Shared PAT Canary

Validation-only file for native Codex existing-PR publishing with the newly generated shared fine-grained PAT stored only as a Codex secret.

CANARY_VALUE=after

Expected repair: change only `CANARY_VALUE=before` to `CANARY_VALUE=after`.
