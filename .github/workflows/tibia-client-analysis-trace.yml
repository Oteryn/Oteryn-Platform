name: Tibia Client Analysis Trace

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/tibia-client-analysis-trace.yml

permissions:
  contents: read

env:
  CONTAINER_NAME: oteryn-tibia-client-analysis

jobs:
  trace:
    name: Capture Tibia launcher UI
    runs-on: oteryn-staging
    timeout-minutes: 10
    steps:
      - name: Verify owned runtime and snapshot staging
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$RUNNER_NAME" == "oteryn-synology-staging" ]]
          [[ "$(docker inspect --format '{{ index .Config.Labels "com.blakinio.owner" }}' "$CONTAINER_NAME")" == "oteryn" ]]
          [[ "$(docker inspect --format '{{ index .Config.Labels "com.blakinio.purpose" }}' "$CONTAINER_NAME")" == "tibia-client-analysis" ]]
          [[ "$(docker inspect --format '{{.State.Status}}' "$CONTAINER_NAME")" == "running" ]]
          docker ps -a --filter 'label=com.docker.compose.project=oteryn-staging' --format '{{.ID}}\t{{.Names}}' | sort > /tmp/oteryn-before.tsv
          test -s /tmp/oteryn-before.tsv

      - name: Capture bounded launcher screenshot
        shell: bash
        run: |
          set -Eeuo pipefail
          docker exec "$CONTAINER_NAME" bash -lc '
            set -Eeuo pipefail
            export DISPLAY=:99 DEBIAN_FRONTEND=noninteractive
            if ! command -v import >/dev/null 2>&1; then
              apt-get update -qq
              apt-get install -y --no-install-recommends imagemagick >/tmp/oteryn-imagemagick-install.log 2>&1
            fi
            wid=$(xdotool search --onlyvisible --name "^Tibia$" 2>/dev/null | head -n1)
            [[ -n "$wid" ]]
            rm -f /tmp/launcher.jpg
            import -display :99 -window "$wid" -resize 400x224 -quality 35 /tmp/launcher.jpg
            test -s /tmp/launcher.jpg
            echo LAUNCHER_SCREENSHOT_BYTES=$(stat -c %s /tmp/launcher.jpg)
            echo LAUNCHER_SCREENSHOT_BASE64_BEGIN
            base64 -w0 /tmp/launcher.jpg
            echo
            echo LAUNCHER_SCREENSHOT_BASE64_END
          '

      - name: Verify canonical staging unchanged
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          docker ps -a --filter 'label=com.docker.compose.project=oteryn-staging' --format '{{.ID}}\t{{.Names}}' | sort > /tmp/oteryn-after.tsv
          if [[ -f /tmp/oteryn-before.tsv ]]; then diff -u /tmp/oteryn-before.tsv /tmp/oteryn-after.tsv; fi
          docker inspect --format 'analysis_name={{.Name}} status={{.State.Status}} exit={{.State.ExitCode}} oom={{.State.OOMKilled}} owner={{ index .Config.Labels "com.blakinio.owner" }} purpose={{ index .Config.Labels "com.blakinio.purpose" }}' "$CONTAINER_NAME"
