name: Tibia Live World Session Verify

on:
  push:
    branches: [ops/oteryn-tibia-client-analysis-20260811]
    paths: [.github/workflows/tibia-client-live-worldmap-attach.yml]

permissions:
  contents: read

env:
  CONTAINER_NAME: oteryn-tibia-client-analysis

jobs:
  verify:
    name: Verify recovered client is actually in world
    runs-on: oteryn-staging
    timeout-minutes: 3
    steps:
      - name: Verify runtime, all client PIDs, sockets and reversible world action
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$RUNNER_NAME" == "oteryn-synology-staging" ]]
          [[ "$(docker inspect --format '{{ index .Config.Labels "com.blakinio.owner" }}' "$CONTAINER_NAME")" == "oteryn" ]]
          [[ "$(docker inspect --format '{{ index .Config.Labels "com.blakinio.purpose" }}' "$CONTAINER_NAME")" == "tibia-client-analysis" ]]
          docker exec -i "$CONTAINER_NAME" bash <<'INNER'
          set -Eeuo pipefail
          export DISPLAY=:99 HOME=/data/home
          mapfile -t pids < <(pgrep -x client || true)
          (( ${#pids[@]} > 0 ))
          echo CLIENT_PIDS=${pids[*]}
          all_tcp=''
          for pid in "${pids[@]}"; do
            rows=$(ss -ntp 2>/dev/null | grep "pid=$pid," || true)
            [[ -z "$rows" ]] || all_tcp+=$'\n'$rows
          done
          local_count=$(printf '%s\n' "$all_tcp" | awk '$5 ~ /127\.0\.0\.1:25344$/ || $5 ~ /\[::1\]:25344$/ {n++} END{print n+0}')
          total_count=$(printf '%s\n' "$all_tcp" | grep -c . || true)
          direct_count=$((total_count-local_count))
          echo ACTIVE_LOCAL_SOCKS_COUNT=$local_count
          echo ACTIVE_DIRECT_TCP_COUNT=$direct_count
          (( direct_count == 0 ))

          best=''; best_area=0; bw=0; bh=0
          for w in $(xdotool search --onlyvisible --name '^Tibia$' 2>/dev/null || true); do
            geom=$(xdotool getwindowgeometry --shell "$w" 2>/dev/null || true)
            width=$(printf '%s\n' "$geom" | sed -n 's/^WIDTH=//p')
            height=$(printf '%s\n' "$geom" | sed -n 's/^HEIGHT=//p')
            [[ -n "$width" && -n "$height" ]] || continue
            area=$((width*height))
            if (( area > best_area )); then best=$w; best_area=$area; bw=$width; bh=$height; fi
          done
          [[ -n "$best" ]]
          echo WINDOW_ID=$best
          echo WINDOW_GEOMETRY=${bw}x${bh}

          import -display :99 -window "$best" /tmp/world-verify-before.png
          crop_w=$(( bw > 620 ? 620 : bw ))
          crop_h=$(( bh > 390 ? 390 : bh ))
          crop_x=$(( (bw-crop_w)/2 ))
          crop_y=$(( (bh-crop_h)/2 ))
          convert /tmp/world-verify-before.png -crop "${crop_w}x${crop_h}+${crop_x}+${crop_y}" /tmp/world-verify-before-vp.png
          xdotool windowactivate --sync "$best" 2>/dev/null || true
          xdotool key --window "$best" Right
          sleep 2
          import -display :99 -window "$best" /tmp/world-verify-after.png
          convert /tmp/world-verify-after.png -crop "${crop_w}x${crop_h}+${crop_x}+${crop_y}" /tmp/world-verify-after-vp.png
          ae=$(compare -metric AE /tmp/world-verify-before-vp.png /tmp/world-verify-after-vp.png null: 2>&1 || true)
          ae=${ae%%.*}
          [[ "$ae" =~ ^[0-9]+$ ]] || ae=0
          echo VIEWPORT_CHANGED_PIXELS_AFTER_RIGHT=$ae
          (( ae > 1000 ))
          xdotool key --window "$best" Left
          sleep 1
          echo IN_WORLD_SESSION_PROVEN=true
          (( local_count > 0 ))
          INNER
