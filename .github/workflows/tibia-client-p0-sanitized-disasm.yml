name: Tibia Client P0 Sanitized Disasm

on:
  push:
    branches: [support/p0-sanitized-disasm-20260816]
    paths: [.github/workflows/tibia-client-p0-sanitized-disasm.yml]

permissions:
  contents: read

env:
  WORK_VOLUME: otclient-runner_runner_work
  EXPECTED_SIZE: '51965216'
  EXPECTED_SHA256: e6c244bd39fe2e0632f6f000efd3147164696efa8e901718668e0442325ff7fe

jobs:
  sanitized-static-evidence:
    runs-on: oteryn-staging
    timeout-minutes: 5
    steps:
      - name: Emit bounded exact-client TPlayerData target bytes
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$RUNNER_NAME" == "oteryn-synology-staging" ]]
          image=$(docker inspect otclient-tibia-login-analysis --format '{{.Image}}')
          docker run --rm --network none --read-only \
            --security-opt no-new-privileges:true \
            -e EXPECTED_SIZE -e EXPECTED_SHA256 \
            --mount type=volume,src="$WORK_VOLUME",dst=/runner-work,readonly \
            --entrypoint /bin/bash "$image" -lc '
              set -Eeuo pipefail
              client=/runner-work/_otclient_tibia_re_state/cache/current-client/client
              [[ "$(stat -c %s "$client")" == "$EXPECTED_SIZE" ]]
              [[ "$(sha256sum "$client" | awk "{print \$1}")" == "$EXPECTED_SHA256" ]]
              # Run 31952152531 proved the executable PT_LOAD has p_offset == p_vaddr
              # and spans every range below. Re-assert the exact segment boundary.
              po=$(od -An -tu8 -j $((64+3*56+8)) -N8 "$client" | tr -d "[:space:]")
              pv=$(od -An -tu8 -j $((64+3*56+16)) -N8 "$client" | tr -d "[:space:]")
              pfs=$(od -An -tu8 -j $((64+3*56+32)) -N8 "$client" | tr -d "[:space:]")
              echo "EXEC_PT_LOAD offset=$po vaddr=$pv filesz=$pfs"
              [[ "$po" == "$pv" ]]
              emit(){
                label="$1"; start=$(( $2 )); stop=$(( $3 )); len=$((stop-start));
                (( pv <= start && stop <= pv+pfs ));
                echo "HEX_${label} VA=$(printf 0x%x "$start")..$(printf 0x%x "$stop") FILEOFF=$start LEN=$len"
                od -An -v -tx1 -j "$start" -N "$len" "$client" | tr -d "[:space:]"; echo
              }
              emit TPLAYERDATA_SLOT3_4 0x843d80 0x844100
              emit TPLAYERDATA_SLOT0 0xd1cb00 0xd1ce00
              emit TPLAYERDATA_SLOT1 0xd2abc0 0xd2b000
              emit TPLAYERDATA_SLOT2 0xd2ee80 0xd2f200
            '
      - name: State static-only boundary
        shell: bash
        run: |
          echo 'STATIC_ONLY=true'
          echo 'CLIENT_EXECUTED=false'
          echo 'PROCESS_MEMORY_ACCESSED=false'
          echo 'NETWORK_ACCESSED=false'
          echo 'CANONICAL_RUNTIME_SESSION_TOUCHED=false'
