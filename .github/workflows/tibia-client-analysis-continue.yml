name: Tibia Client Analysis Continue

on:
  push:
    branches:
      - ops/oteryn-tibia-client-analysis-20260811
    paths:
      - .github/workflows/tibia-client-analysis-continue.yml

permissions:
  contents: read

concurrency:
  group: oteryn-tibia-client-analysis-continue
  cancel-in-progress: false

jobs:
  continue:
    name: Inspect exact Tibia launcher metadata
    runs-on: oteryn-staging
    timeout-minutes: 15
    env:
      CONTAINER_NAME: oteryn-tibia-client-analysis
    steps:
      - name: Verify exact owned runtime and staging snapshot
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$RUNNER_NAME" == "oteryn-synology-staging" ]]
          owner="$(docker inspect --format '{{ index .Config.Labels "com.blakinio.owner" }}' "$CONTAINER_NAME")"
          purpose="$(docker inspect --format '{{ index .Config.Labels "com.blakinio.purpose" }}' "$CONTAINER_NAME")"
          state="$(docker inspect --format '{{.State.Status}}' "$CONTAINER_NAME")"
          [[ "$owner" == "oteryn" && "$purpose" == "tibia-client-analysis" && "$state" == "running" ]]
          docker ps -a --filter 'label=com.docker.compose.project=oteryn-staging' --format '{{.ID}}\t{{.Names}}' | sort > /tmp/oteryn-before.tsv
          test -s /tmp/oteryn-before.tsv

      - name: Inspect local and official launcher metadata
        shell: bash
        run: |
          set -Eeuo pipefail
          docker exec "$CONTAINER_NAME" bash -lc '
            set -Eeuo pipefail
            mkdir -p /data/direct
            cd /data/direct
            localmeta="/data/home/.local/share/CipSoft GmbH/Tibia/launchermetadata.json"
            echo "=== LOCAL LAUNCHER METADATA ==="
            test -s "$localmeta"
            cat "$localmeta"
            echo
            echo "=== OFFICIAL TIBIA METADATA ==="
            u="https://static.tibia.com/launcher/tibiametadata.json"
            curl --compressed -fLC - -e "$u" "$u" -o tibiametadata.json
            cat tibiametadata.json
            echo
            echo "=== CURRENT CLIENT VERSION ==="
            vurl="https://static.tibia.com/launcher/tibiaclient-linux-current/package.json.version"
            curl --compressed -fLC - -e "$vurl" "$vurl" -o package.json.version
            cat package.json.version
            echo
            echo "=== ASSET HASH ==="
            aurl="https://static.tibia.com/launcher/assets-current/assets.json.sha256"
            curl --compressed -fLC - -e "$aurl" "$aurl" -o assets.json.sha256
            cat assets.json.sha256
            echo
            echo "=== LOCAL PACKAGE TREE ==="
            find "/data/home/.local/share/CipSoft GmbH/Tibia" -maxdepth 8 -type f -printf "%s %p\n" 2>/dev/null | sort -nr | head -n 200
          '

      - name: Verify canonical staging unchanged
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          docker ps -a --filter 'label=com.docker.compose.project=oteryn-staging' --format '{{.ID}}\t{{.Names}}' | sort > /tmp/oteryn-after.tsv
          if [[ -f /tmp/oteryn-before.tsv ]]; then diff -u /tmp/oteryn-before.tsv /tmp/oteryn-after.tsv; fi
          docker inspect --format 'analysis_name={{.Name}} status={{.State.Status}} owner={{ index .Config.Labels "com.blakinio.owner" }} purpose={{ index .Config.Labels "com.blakinio.purpose" }}' "$CONTAINER_NAME"
