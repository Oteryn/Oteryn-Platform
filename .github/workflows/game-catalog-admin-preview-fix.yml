name: Deprecated Game Catalog Admin Preview Helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  no-op:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary write helper disabled; remove this workflow in the next task step.'
