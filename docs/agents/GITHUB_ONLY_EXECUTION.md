# Platform Repository-Native Execution Delta

The bound META policy controls shared GitHub and execution routing. For Platform, repository-native GitHub operations may be used only for `Oteryn/Oteryn-Platform` under the governing task. They do not authorize server-repository inspection, production, protected environments, credentials or live payments.

Use current remote Issue/PR/head/check state for lifecycle decisions. A missing local checkout is not proof that repository-native operations are unavailable. Do not create temporary workflows when existing checks or connector operations can prove the result. `platform-gate` and current protected integration controls remain required.
